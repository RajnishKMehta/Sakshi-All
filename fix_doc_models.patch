--- 01_sdk/sakshi-sdk/src/main/kotlin/rajnishkmehta/sakshi/sdk/api/models/FileCopyRequest.kt
+++ 01_sdk/sakshi-sdk/src/main/kotlin/rajnishkmehta/sakshi/sdk/api/models/FileCopyRequest.kt
@@ -3,10 +3,10 @@
 /**
  * Represents a file copy payload request submitted to the Vault application.
  *
- * @property fileId Unique identifier for this file submission. Must contain only lowercase a-z, digits 0-9, underscore, or hyphen, and not exceed 255 characters.
+ * @property fileId Unique identifier for this file submission. Must contain only lowercase a-z, digits 0-9, underscore, or hyphen, and be between 8 and 128 characters long.
  * @property uri Content Uri or file reference pointing to the source file.
  * @property mediaType Media type of file (e.g. "PHOTO", "VIDEO", "AUDIO", "OTHER").
- * @property fileExtension The file extension for the file (e.g. "jpg", "png"). Must not exceed 50 characters, contain spaces, or start/end with a dot.
+ * @property fileExtension The file extension for the file (e.g. "jpg", "png"). Must be between 1 and 50 characters long, not contain spaces, or start/end with a dot.
  * @property timestampEpochMs Epoch timestamp in milliseconds when the file was captured.
  * @property metadata Optional key-value metadata associated with the file.
  */
--- 01_sdk/sakshi-sdk/src/main/kotlin/rajnishkmehta/sakshi/sdk/api/models/AVSyncRequest.kt
+++ 01_sdk/sakshi-sdk/src/main/kotlin/rajnishkmehta/sakshi/sdk/api/models/AVSyncRequest.kt
@@ -3,10 +3,10 @@
 /**
  * Request payload sent by Camera application to Vault to initiate incremental audio/video synchronization.
  *
- * @property fileId Unique identifier generated by Camera for the audio/video recording. Must contain only lowercase a-z, digits 0-9, underscore, or hyphen, and not exceed 255 characters.
+ * @property fileId Unique identifier generated by Camera for the audio/video recording. Must contain only lowercase a-z, digits 0-9, underscore, or hyphen, and be between 8 and 128 characters long.
  * @property uri Content Uri or file reference pointing to the active recording file.
  * @property mediaType Media type of recording (e.g. "PHOTO", "VIDEO", "AUDIO", "OTHER").
- * @property fileExtension The file extension for the recording (e.g. "mp4", "m4a"). Must not exceed 50 characters, contain spaces, or start/end with a dot.
+ * @property fileExtension The file extension for the recording (e.g. "mp4", "m4a"). Must be between 1 and 50 characters long, not contain spaces, or start/end with a dot.
  * @property startTimestampEpochMs Epoch timestamp in milliseconds when recording started.
  * @property metadata Optional key-value metadata accompanying the sync request.
  */
